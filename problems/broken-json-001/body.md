## 問題

この問題は、壊れた `problem.json` を検出できるか確認するためのテスト用問題です。

## 制約

この問題に制約はありません。

## 入力

この問題に入力はありません。

## 出力

文字列 `broken json test` を 1 行で出力してください。

## ヒント

このフォルダの主な目的は、`problem.json` の読み込みエラーを検証することです。
