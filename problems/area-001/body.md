## 問題

長方形の縦 `h` と横 `w` が与えられます。  
長方形の面積を出力してください。

## 制約

- `1 ≤ h, w ≤ 100`

## 入力

入力は次の形式で標準入力から与えられます。

```text
h w
```

## 出力

長方形の面積 `h * w` を 1 行で出力してください。

## ヒント

掛け算の結果をそのまま `printf` で出力します。
