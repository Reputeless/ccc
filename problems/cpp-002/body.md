## 問題

あるゲームを何日か続けて遊びました。  
各日の結果は、勝ちなら `o`、負けなら `x` で記録されています。

結果を表す文字列 `s` が与えられるので、`o` が連続している区間の長さの最大値を求めてください。

## 制約

- `s` の長さは `1` 以上 `100` 以下
- `s` は `o` と `x` だけからなる

## 入力

入力は次の形式で標準入力から与えられます。

```text
s
```

## 出力

`o` が連続している区間の長さの最大値を出力してください。

## ヒント

左から 1 文字ずつ見て、今何個連続しているかを数えると考えやすいです。  
`x` が来たら連続数を 0 に戻します。
