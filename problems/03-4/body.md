## 問題

標準入力から長方形の幅 `w` と高さ `h` を読み込み、長方形の面積を出力してください。

## 制約

- `1 ≤ w, h ≤ 100`

## 入力

入力は次の形式で標準入力から与えられます。

```text
w h
```

## 出力

長方形の面積 `w * h` を 1 行で出力してください。

## ヒント

`scanf` で `w` と `h` を読み込み、そのあと `w * h` を `printf` で出力します。
