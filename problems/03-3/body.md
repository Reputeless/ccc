## 問題

標準入力から 2 つの整数 `a`, `b` を読み込み、左右を入れ替えて出力してください。

## 制約

- `1 ≤ a, b ≤ 100`

## 入力

入力は次の形式で標準入力から与えられます。

```text
a b
```

## 出力

`b`、半角空白、`a` の順に 1 行で出力してください。

## ヒント

まず `scanf` で 2 つの整数を読み込みます。  
出力するときは、入力した順ではなく逆順に `printf` へ渡します。
