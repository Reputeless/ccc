## 問題

標準入力から 2 つの整数 `a`, `b` を読み込み、その和を出力してください。

## 制約

- `1 ≤ a, b ≤ 100`

## 入力

入力は次の形式で標準入力から与えられます。

```text
a b
```

## 出力

`a + b` の値を 1 行で出力してください。

## ヒント

`scanf` で 2 つの整数を読み込み、そのあと `printf` で和を出力します。  
和は `a + b` と書けます。
