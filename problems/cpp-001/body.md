## 問題

文字列 `Hello, C++!` を 1 行で出力してください。

## 制約

この問題に制約はありません。

## 入力

この問題に入力はありません。

## 出力

`Hello, C++!` を 1 行で出力してください。

## ヒント

C++23 では、`std::println` を使うと改行付きの出力を簡潔に書けます。

```cpp
#include <print>

int main()
{
	std::println("Hello, C++!");
}
```
