## 考え方

この問題では、`****` という同じ行を 4 回出力します。  
まずは 1 行を正しく書けることを確認して、そのあと同じ `puts("****");` を 4 回並べると考えると分かりやすいです。

`*` は「アスタリスク」と読みます。  
プログラムでは、文字列の中にそのまま `*` を書けます。

## 模範解答

```c
#include <stdio.h>

int main()
{
	puts("****");
	puts("****");
	puts("****");
	puts("****");
}
```
