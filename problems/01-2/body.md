## 問題

1 行目に `Apple`、2 行目に `Banana` を出力してください。

## 制約

この問題に制約はありません。

## 入力

この問題に入力はありません。

## 出力

次の 2 行をこの順に出力してください。

- 1 行目: `Apple`
- 2 行目: `Banana`

## ヒント

`puts` を 2 回使うと、それぞれの文字列を別の行に表示できます。

```c
#include <stdio.h>

int main()
{
	puts("Apple");
	puts("Banana");
}
```
