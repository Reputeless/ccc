# 問題

整数 `N` が与えられます。  
`1` から `N` までの整数を、1 行に 1 つずつ出力してください。

## 制約

- `1 <= N <= 10`

## 入力

入力は次の形式で標準入力から与えられます。

```text
N
```

## 出力

`1` から `N` までを改行区切りで出力してください。

## ヒント

`for` 文で `1` から順番に回してみましょう。
