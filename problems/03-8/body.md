## 問題

標準入力から 4 桁の整数 `n` を読み込み、一の位から順に 1 行ずつ出力してください。

## 制約

- `1000 ≤ n ≤ 9999`

## 入力

入力は次の形式で標準入力から与えられます。

```text
n
```

## 出力

一の位、十の位、百の位、千の位の順に、1 行ずつ出力してください。

## ヒント

一の位は `n % 10` で取り出せます。  
十の位は `n / 10 % 10` のように、先に `/` で右へずらしてから `% 10` を使います。
