## 問題

標準入力から整数 `n` を読み込み、`1` から `n` までの整数を 1 行に 1 つずつ出力してください。

## 制約

- `1 ≤ n ≤ 10`

## 入力

入力は次の形式で標準入力から与えられます。

```text
n
```

## 出力

`1` から `n` までを改行区切りで出力してください。

## ヒント

`for` 文で `1` から順番に回してみましょう。
