## 問題

整数 `x` が与えられます。  
`x` が `0` 以上なら `non-negative`、そうでなければ `negative` を出力してください。

## 制約

- `-100 ≤ x ≤ 100`

## 入力

入力は次の形式で標準入力から与えられます。

```text
x
```

## 出力

条件に応じて、次のどちらかを 1 行で出力してください。

- `non-negative`
- `negative`

## ヒント

`if` と `else` を使う練習用の問題です。
