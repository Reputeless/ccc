## 問題

次の 2 つの計算結果を、1 行ずつ表示してください。

- `123 + 456`
- `123 - 456`

## 制約

この問題に制約はありません。

## 入力

この問題に入力はありません。

## 出力

次の順に 2 行で出力してください。

- 1 行目: `123 + 456` の結果
- 2 行目: `123 - 456` の結果

## ヒント

`printf("%d\n", ...);` を使うと、整数を 1 行ずつ表示できます。  
`+` や `-` の計算は、そのまま `printf` の中に書けます。
