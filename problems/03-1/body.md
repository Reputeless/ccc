## 問題

標準入力から 1 つの整数 `x` を読み込み、その 2 倍の値を出力してください。

## 制約

- `1 ≤ x ≤ 100`

## 入力

入力は次の形式で標準入力から与えられます。

```text
x
```

## 出力

`x` の 2 倍の値を 1 行で出力してください。

## ヒント

まず `scanf` で整数を 1 つ読み込み、そのあと `printf` で 2 倍の値を出力します。  
2 倍は `x * 2` と書けます。
