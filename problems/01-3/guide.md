## 考え方

C 言語では、文字列の中に改行やタブのような特殊な文字を入れたいときに、エスケープシーケンスを使います。  
この問題では、意味から対応するエスケープシーケンスを答えます。

## 各小問の解答

- 改行: `\n`
- タブ: `\t`
- ダブルクォーテーション: `\"`
- バックスラッシュ: `\\`

## 備考

`\"` と `\\` は、`"` と `\` をそのまま文字列の中に書きたいときに使います。
