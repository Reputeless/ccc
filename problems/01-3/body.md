## 問題

各小問について、指定された意味に対応するエスケープシーケンスを入力してください。  
この問題では、`\n`、`\t`、`\"`、`\\` を扱います。

## 入力

この問題では、コードの入力はありません。  
各小問に対する解答を、画面上の入力欄に記入します。

## 出力

この問題では、通常の出力はありません。  
各小問の正しいエスケープシーケンスを入力して判定します。

## ヒント

- エスケープシーケンスは、バックスラッシュ `\` と文字を組み合わせて書きます
- どの小問も、半角で入力してください
