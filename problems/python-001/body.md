## 問題

文字列 `Hello, Python!` を 1 行で出力してください。

## 制約

この問題に制約はありません。

## 入力

この問題に入力はありません。

## 出力

`Hello, Python!` を 1 行で出力してください。

## ヒント

Python では、`print` を使うと 1 行の出力を簡潔に書けます。

```python
print("Hello, Python!")
```
