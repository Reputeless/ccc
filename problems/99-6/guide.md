## 考え方

記号の読み方は、プログラムを書くときの説明や質問でよく使います。  
この問題では、`*`、`/`、`;`、`,` の読み方を答えます。

## 各小問の答え

- `*` は `アスタリスク`
- `/` は `スラッシュ`
- `;` は `セミコロン`
- `,` は `カンマ` または `コンマ`

## 備考

記号によっては、授業や本によって呼び方が複数使われることがあります。  
この問題では、`,` について `カンマ` と `コンマ` のどちらも正解にしています。
